# Rename App Display Name

The app display name (shown on home screen / launcher) is set separately per platform.

## Use `rename_app` package
```bash
flutter pub add rename_app --dev
flutter pub run rename_app:main all="Your App Name"
flutter pub remove rename_app
```
